use @piton/belay

from ../Conventions import Conventions

export skill ReviewChange:
    description: Review a change against the project's conventions.
    useWhen: reviewing a change before it is merged
    prompt:
        Read @{Conventions} first, then review the change against it. Report
        each place it departs from them, with the file and line.
