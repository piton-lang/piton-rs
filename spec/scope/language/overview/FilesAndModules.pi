export anchor FilesAndModules:
    SourceFilesAndModules:
        Piton files carry the .pi extension.

        Like Python, Node, and many other languages, directories become modules
        with an index.pi file that exports symbols imported from other files.
        That means that when doing an import you can point at the directory
        rather than at a specific file.
