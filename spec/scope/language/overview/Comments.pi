export anchor Comments:
    comments:
        Comments are line-only. There are no block comments. Comments are
        created with "//".

        ```piton
        // This is a comment
        myVariable: 42 // This is also a comment
        ```

        `piton format` will always put a space between the "//" and the comment
        text, so you might as well get used to doing it yourself.
