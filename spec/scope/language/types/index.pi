from ./Anchors import AnchorType
from ./Booleans import Booleans
from ./Collections import Collections
from ./ComplexTypes import ComplexTypes
from ./Inference import Inference
from ./Null import Null
from ./Numbers import Numbers
from ./Overview import TypesOverview
from ./Reference import Reference
from ./SimpleTypes import SimpleTypes
from ./Strings import Strings

export anchor Types:
    overview: @{TypesOverview}
    numbers: @{Numbers}
    strings: @{Strings}
    booleans: @{Booleans}
    null: @{Null}
    collections: @{Collections}
    userDefined: @{AnchorType}
    inference: @{Inference}
    reference: @{Reference}
    simpleTypes: @{SimpleTypes}
    complexTypes: @{ComplexTypes}
