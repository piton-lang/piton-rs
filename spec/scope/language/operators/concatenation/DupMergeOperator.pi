use ../lib/Operator

export operator DupMergeOperator:
    description:
        The merge operator combines two values into a single value,
        preserving all elements, not removing duplicates.

    symbol: ++
