from ./cli import Cli
from ./project-config import ProjectConfig
from ./frameworks import Frameworks
from ./editors import
    Emacs,
    Helix,
    JetBrains,
    Kate,
    NeoVim,
    Sublime,
    Vim,
    VsCode,
    Zed

from ./vite import VitePlugin
from ./astro import AstroPlugin

export anchor Tooling:
    cli: {Cli}
    projectConfig: {ProjectConfig}
    frameworks: {Frameworks}
    editors:
        - {Emacs}
        - {Helix}
        - {JetBrains}
        - {Kate}
        - {NeoVim}
        - {Sublime}
        - {Vim}
        - {VsCode}
        - {Zed}
    consumption:
        - {AstroPlugin}
        - {VitePlugin}
