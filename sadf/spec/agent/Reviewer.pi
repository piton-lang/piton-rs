use @piton/belay

from ./ReviewChange import ReviewChange

export agent Reviewer:
    description: Reviews a change against the project's conventions.
    role: careful reviewer who reads a change before judging it
    prompt:
        Review the change you are given the way @{ReviewChange} describes, and
        report what you find without changing any files.
